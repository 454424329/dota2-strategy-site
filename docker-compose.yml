services:
  app:
    build: .
    container_name: dota2-strategy
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_SITE_URL=${NEXT_PUBLIC_SITE_URL:-http://59.110.160.9:3000}
      - DATABASE_URL=postgresql://dota2:dota2_community_2026@harbor-db:5432/dota2
      - AUTH_SECRET=gP4s/sUPvy+/IojgEjNn5NdzyR4ObY9taNulItObYMc=
    networks:
      - harbor_harbor

networks:
  harbor_harbor:
    external: true
